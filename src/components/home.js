import React from 'react';
import ProjectsList from './views/projects-list';

const Home = React.createClass({
  render: function() {
    return (
      <ProjectsList />

    );
  }
});

export default Home;
